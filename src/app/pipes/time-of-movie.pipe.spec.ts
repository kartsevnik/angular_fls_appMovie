import { TimeOfMoviePipe } from './time-of-movie.pipe';

describe('TimeOfMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeOfMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
